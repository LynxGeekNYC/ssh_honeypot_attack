# ssh_honeypot_attack
SSH Honeypot that detects SSH connections, shows real banner and scans the attackers open ports for counter-attacks

# Library: sudo apt-get install libboost-all-dev
# Compile: g++ -o ssh_honeypot ssh_honeypot.cpp -lssh -lboost_system
